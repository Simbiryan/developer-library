extends layout

block content

  h1 Жанр: #{genre.name}
  
  div(style='margin-left:20px;margin-top:20px')

    h4 Книги
    
    dl
      each book in genre_books
        dt 
          a(href=book.url) #{book.title}
        dd #{book.summary}

      else
        p Книг с данным жанром не найдено